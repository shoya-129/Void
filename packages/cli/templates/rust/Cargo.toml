[package]
name = "{{name}}"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
void-sdk-rust = "*"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
